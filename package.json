{
  "name": "pr-timeline",
  "version": "0.1.0",
  "description": "Atomize a PR or large commit into replayable atomic commits, then step through them in an editor-grade viewer",
  "type": "module",
  "bin": {
    "pr-timeline": "bin/pr-timeline.mjs"
  },
  "scripts": {
    "serve": "node bin/pr-timeline.mjs serve",
    "test": "node --test"
  },
  "dependencies": {
    "monaco-editor": "^0.52.2"
  }
}
